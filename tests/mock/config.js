export const targetId = 'player-placeholder-skip.spec';

export const config = {
    targetId: targetId,
    provider: {},
    playback: {
        autoplay: true
    },
    plugins: {
        uiManagers: {
        }
    }
};